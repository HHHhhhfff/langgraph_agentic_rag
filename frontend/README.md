# LangGraph Agentic RAG 前端工作台

这是基于参考设计整理出的前端工程，服务于当前 `langgraph_agentic_rag` 项目。界面重点覆盖文档入库、索引管理、RAG 问答、证据引用、检索诊断、评测与配置管理。

当前版本是前端原型与静态数据演示，不会改动后端、CLI、评测或索引构建逻辑。

## 运行方式

安装依赖：

```bash
npm install
```

启动开发服务器：

```bash
npm run dev
```

构建静态资源：

```bash
npm run build
```
